# 交接 —— dsh-api-dashboard 价格校准与会话消耗估算修复

- 日期：2026-09-22
- 工作副本：`/root/dsha-api-dashboard`（从 GitHub `main` clone）
- 当前 HEAD：`d32f941`，**本地领先 origin/main 2 个提交，未推送**
- 状态：计算口径与价格表已收口；**Gemini 一家未取证**（外部网络阻塞，见 §6）

---

## 1. 一句话状态

按各厂商官方定价页重核了**八家**价格表（DeepSeek / 智谱 / 阿里百炼 / MiniMax / 阶跃 / Kimi / OpenAI / Anthropic），
修掉**三处估算口径错误**（峰谷时间基准、法定节假日、缓存写入价）与**五个具体缺陷**，
19 个测试文件全绿，版本已闭环到 `v1.4.5`。**未推送**（AGENTS.md 铁律：推送前问维护者）。

---

## 2. 环境与工作副本

| 对象 | 路径 | 说明 |
|---|---|---|
| 工作副本（本次改动在这里） | `/root/dsha-api-dashboard` | 从 GitHub clone；`main` 领先 origin 2 个提交 |
| 运行中的插件 | `/root/.dsh/profiles/web/node_modules/dsh-api-dashboard` | **npm 装的 1.4.4**，非 link |
| 宿主 | `/usr/local/lib/node_modules/@deepseek-ai/dsh` | 0.1.6-alpha.2 |
| 状态文件 | `/root/.dsh/dsh-api-dashboard.json` | 用户配置持久化在此 |

### ⚠️ 两条必须先知道的

1. **`node_modules` 软链是跑测试的前提**（AGENTS.md 安装须知第 3 步）。本次一开始 `test-v141-fixes.mjs` 挂了 10 条，
   根因就是少了它：
   ```sh
   ln -sfn /usr/local/lib/node_modules/@deepseek-ai/dsh/node_modules /root/dsha-api-dashboard/node_modules
   ```
   缺它时症状是 `ERR_MODULE_NOT_FOUND`（`package_json_reader`），**看起来像产品 bug，其实不是**。

2. **运行中的 1.4.4 被本地改过两处**（为了让维护者当下生效，见 §5.2）。
   `npm` 重装该包会**覆盖掉这些改动** —— 正式修法是把插件改成 `link:/root/dsha-api-dashboard`。

---

## 3. 已完成的改动（两个提交）

### 3.1 `5aa3441` — v1.4.5：估算口径三处修正 + 八家价格表重核

**① 峰谷改按「事件发生时刻」计价**（`src/index.js`，影响最大）

- 旧实现只在 `summarize()` 里调 `resolveModelPrice(cfg, model)`，**没传时间戳** → 取 `Date.now()`
  → 整段历史消耗都按「打开面板那一刻」的时段计价。**DeepSeek 峰谷差整整 2 倍**，谷时跑的 token
  在峰时查看会被翻倍。而本插件的主力模型就是 `deepseek-flash`（实测会话里 100% 是它）。
- 修法：`foldUsage` 用事件自带的 **`event.time`** 判定该笔属于峰还是谷，累积进新增的 `phaseBuckets`；
  `summarize` 按峰/谷两套单价分别求和。`resolveModelPrice` 增加第 4 个参数 `phase`（`'peak'`/`'offPeak'`，传 null 才是旧行为）。
- `stateVersion` **3 → 4**。版本不匹配时框架从 `seq 0` 重放，重放时 `event.time` 仍在，所以分相结果正确；
  读不到分相的老缓存（冷路径）自动回落旧行为，不崩也不错数。
- 相关：`phaseOfModel` / `phaseAdd` / `phaseSub` / `clampMs`（后者见 §3.2）。

**② 法定节假日全天算谷时**

- 官方**英文**定价页明文：`Peak hours are 01:00-04:00 and 06:00-10:00 UTC, Monday through Friday,
  excluding Chinese public holidays ... including weekends and Chinese public holidays in full`。
  中文页只写「其余为空闲时段」，容易漏掉这半句。
- 表：`CN_HOLIDAY_RANGES`（2026 年 7 个区间，共 **33 天**），来源国务院办公厅 2025-11-04 通知
  （`gov.cn/zhengce/zhengceku/202511/content_7047091.htm`）。漏掉它这 33 天全部翻倍。
- **每年 11 月国务院办公布次年安排后必须补表**；未收录年份按「无节假日」回落（保守）。
- 调休上班的周末（2026-02-14 / 02-28 / 05-09 / 09-20 / 10-10）**不列入表内** —— 官方口径是
  `Monday through Friday` 字面，周末本身已是谷时。

**③ 缓存写入单独计价**

- 新增可选档 `cacheWrite`。官方 Anthropic 一律 = 输入价 **×125%**
  （Opus 5 `$6.25/$5`、Sonnet 5 `$2.50/$2`、Haiku 4.5 `$1.25/$1`、Fable 5.1 `$12.50/$10` 逐项吻合）。
- `summarize` 的 `costOf` 优先用 `cacheWrite`，未提供则回落 `cacheMiss`（旧行为不变）。
- 🔴 **`convertPrice` 会静默丢掉未声明的字段** —— 新加的 `cacheWrite` 第一次就是被它吞掉的
  （算出来仍是 $5 而非 $6.25）。已改为显式带过。**以后往价格表加字段，务必同步改 `convertPrice`。**

**④ 价格表（`MODEL_PRICES` / `V4_RATES`）**

补录（全部有官方原文）：
`gpt-6-astra`、`claude-fable-5.1` / `claude-fable-5` / `claude-opus-4-8` / `claude-sonnet-4-5`、
`glm-5.3-flashx` / `glm-5` / `glm-4.7` / `glm-4.7-flashx` / `glm-4.5-air` / `glm-4-air-250414` / `glm-4-long` / `glm-4-flashx`、
`minimax-m3`、`step-5-preview` / `step-1o-turbo-vision`、
`qwen3.8-max-prime` / `qwen3.8-omni-flash` / `qwen3.5-plus`。

修正：
- `gpt-5.6-sol` **4/20 → 5/30**（官方页现价；旧值记的是"促销价"，官方页已无促销标记 → 按 AGENTS.md 红线改用官方现价）
- `qwen-plus` cacheHit **0.4 → 0.08**、`qwen-turbo` **0.15 → 0.03**（官方规则是输入 ×10%；
  同表另外 8 个 qwen 条目都严格是 10%，只有这两个是 50% → 旧值无依据，高估缓存读 5 倍）
- `glm-4-flashx` **键名错配**：官方 `GLM-4-Flash-250414` 是**免费**、`GLM-4-FlashX-250414` 才是 ¥0.1/¥0.1，
  旧表把后者的价挂在前者键上，而 `glm-4-flashx-250414` 没有任何键能收它 → 一直落到 `defaultPrices`（USD $1/$2 ≈ ¥7/¥14）
- DeepSeek 注释更正：官方注(2) 已改为「9-14 之后**继续提供** V4 Pro，计费方式不变」，旧注释「全部路由到 Flash」作废

标注（官方已下线 / 已不列，**保留仅为旧会话估算占位，非官方价**）：
`moonshot-v1-*` + `kimi-k2.5`（2026-08-31 全平台下线，调用 404）、`qwen2.5-72b-instruct`（2026-05-13 下线）、
`gpt-5.3-codex`、`step-1-flash/8k/32k`、腾讯混元三条、豆包 Seed 2.0 全系。

### 3.2 `d32f941` — 修「点设置抽搐一下」+ 刷新间隔 300 秒

**① 点设置抽搐（维护者反馈「每次点设置都稳定复现，从看板面板里面点」）**

- 根因：`openSettings` 里 `setView("bar")` 与 `setSettingsOpen(true)` **同帧生效** —— 从看板点设置时
  `DashboardDrawer` 被**当场卸载**、`SettingsModal` 同时挂载，两块全屏遮罩一换一，而看板那层
  `.dshadb_scrim` 只有入场动画（`dshadb-fadein`）、**没有出场动画** → 视觉上「啪」一下。
- 与「长按状态条打开设置不抽」完全吻合（那时 `view` 本就是 `"bar"`，`setView` 无变化）。
- 修法：打开设置时**不收看板**（设置面板照常盖在上面），关闭设置时再收。交互结果不变
  （`onClose` 回状态条、`onBack` 回看板）。

**② 刷新间隔 300 秒**

- 根因：`cordis.patch.yml` 里写死 `refreshIntervalMs: 300000`，而 `sanitizePersistedShape`
  **只夹「状态文件读出来的值」**，从 `config`（这份 patch / profile 配置）进来的值**没有任何夹取**。
  首次启动（状态文件里还没该字段）时 300000 直达 runtimeConfig —— 而 `clampRefreshSec` 上限明明是 60 秒。
- 修法：新增 `clampMs` 给 config 路径补上同一道闸门（1000~60000）；patch 值 `300000/30000` → `5000/5000`。

---

## 4. 关键根因速查（踩坑记录）

| 症状 | 根因 | 修法 |
|---|---|---|
| `test-v141-fixes` 挂 10 条 | 工作副本缺 `node_modules` 软链 | 建软链（§2） |
| 谷时跑的 token 在峰时被翻倍 | `summarize` 用 `Date.now()` 而非事件时间 | `phaseBuckets` 分相（§3.1①） |
| 节假日期间消耗翻倍 | 未排除中国法定节假日 | `CN_HOLIDAY_RANGES`（§3.1②） |
| 缓存写入价偏低 | `cacheWriteTokens` 并入 `cacheMiss` | `cacheWrite` 档（§3.1③） |
| 新价格字段改了不生效 | **`convertPrice` 静默丢弃未声明字段** | 显式带过（§3.1③🔴） |
| `glm-4-flashx-*` 比真价贵 140 倍 | 键名错配 + 无键能收 | 补 `glm-4-flashx` 键（§3.1④） |
| qwen-plus/turbo 缓存读高估 5 倍 | cacheHit 写成输入价的 50% | 按官方 10% 规则改（§3.1④） |
| **点设置抽搐一下** | 同帧卸载看板 + 挂载设置 | 打开时不收看板（§3.2①） |
| **刷新间隔 300 秒** | patch 写死 + config 路径无夹取 | `clampMs` + 改 patch（§3.2②） |

---

## 5. 测试与验证

### 5.1 跑法

```sh
cd /root/dsha-api-dashboard
for f in test/*.mjs; do node "$f" || echo "FAIL $f"; done
```

当前：**19 个测试文件全绿**。断言计数：

| 文件 | 断言数 | 本次变化 |
|---|---|---|
| `test-pricing.mjs` | 119 | 86 → 119（新增条目 / 阿里 10% 规则 / 缓存写入 125% / 海外两家官方值） |
| `test-cost-projection.mjs` | 21 | 10 → 21（T11 峰谷分相 / T12 缺 time 回落 / T13 节假日 / T14-T15 缓存写入） |
| `test-v141-fixes.mjs` | 87 | 80 → 87（H4b2-H4b6 夹取 / S1-S2 设置面板不瞬删看板） |
| `test-bar.mjs` | 93 | 未变 |

另做过**真实会话端到端重放**（1128 个事件，逐帧解开 `session.v3.jsonl.zstd`）：
`deepseek-flash` 消耗 ¥1.470982，与手算 `0.02×27.144576 + 1×0.172702 + 4×0.188847` 逐项吻合；
同批数据里 `cacheWrite = 0`，证实 **DeepSeek 无缓存写入费**（所以 §3.1③ 对当前主力模型无影响）。

### 5.2 运行中实例（npm 1.4.4）被直接改了两处

为了让维护者**当下**就摆脱 300 秒与抽搐，直接改了已安装目录：

- `.../dsh-api-dashboard/client/client.js` —— 同 §3.2①
- `.../dsh-api-dashboard/cordis.patch.yml` —— `refreshIntervalMs: 5000`

⚠️ **npm 重装会覆盖**。正式做法是 `dsh plugin --profile web add link:/root/dsha-api-dashboard`。

客户端改动需**刷新页面**；DSH 若仍缓存旧客户端模块，需**重启 dsh web GUI**（会断当前会话，注意时机）。

### 5.3 线上运行值（已生效）

`/root/.dsh/dsh-api-dashboard.json` 现为 `refreshIntervalMs = 30000`（30 秒）、`clientPollIntervalMs = 30000`。
注入用的 cookie 在 `/tmp/dsh-cookie.txt`（合成方式见下，**24h 有效**）：

```
name = 'dsh-auth-' + base64url(sha256('127.0.0.1:3080'))
value = 'v1.' + base64url(JSON{version,authority,issuedAt,expiresAt}) + '.' + base64url(hmacSHA256(secret, body))
secret = ~/.dsh/.credentials.yaml → records['client-connection/browser-session'].payload.secret (base64url 解码)
```

改刷新间隔：`POST /api-dashboard/config {"refreshIntervalSec":30}`（带上述 cookie）。

---

## 6. 未完成事项

### 6.1 Gemini 取证（唯一阻塞项）

`ai.google.dev` / `cloud.google.com` 在本容器 **TCP 直连失败**；试过 `developers.google.com` /
`gemini.google.com` / `google.dev` 全不通，而 `deepmind.google`、`storage.googleapis.com` 可达
→ 是**针对性封锁**，不是全断。r.jina.ai / allorigins / corsproxy 等文本代理也都不通。

维护者已开启魔法网络，但**未覆盖到这个容器**（`ai.google.dev` 仍超时）。
**需要确认魔法是全局生效还是需给容器配 `HTTPS_PROXY`**；拿到官方原文前**不要改 Gemini 的值**（红线：不许编造）。

有价值的技巧（本次靠它拿下 OpenAI / Anthropic，**不需要代理**）：

> 这两家的价格**不在 `<table>` 里**，而藏在页面 `<script>` 的**双层转义 JSON** 中。
> 直接 fetch 页面后依次还原（双反斜杠→单反斜杠、`\n`→换行、`\"`→`"`）即可拿到官方值：
> - OpenAI：`"pricingGroups":[... "Input:\n$X / 1M tokens\n\nCached input:\n$Y ...\n\nOutput:\n$Z" ...]`
> - Anthropic：`ApiTab-module__*__priceLabel` 之后，且**额外给出 Prompt caching 的 Read / Write 两档**

### 6.2 其他

- **豆包 Seed 2.0 全系**：火山方舟官方文档/定价页都是 SPA，fetch 只拿到壳（0 表）。
  取证路径：用带网络的浏览器打开 `volcengine.com/docs/82379/1544106` 抄表回填。
- **B 栏平台解析**（OpenRouter / siliconflow / Novita / one-api / xAI）：字段名存疑，**需真实 key 实测**（AGENTS.md §三）。
- **README 过时描述**：写着「设置齿轮直接出现在状态条右侧」，实际 v0.5.0 起状态条入口已换成
  **长按 500ms**（齿轮只保留在看板里）。维护者已知悉，等其点头再改。
- **分档价未实现**：GLM-5 系 `[0,32K)`/`≥32K`、Qwen 256K 档等，本表一律取**更贵的档**（保守）。表结构是单值，支持分档需改结构。
- **版本 `1.4.5` 未发布**；推送命令（**需维护者授权**）：`git push origin main` + `git tag v1.4.5 && git push origin v1.4.5`
  （npm 发布走 Trusted Publishing / OIDC，见 `docs/RELEASING.md`）。

---

## 7. 红线（改这块必读）

沿用 AGENTS.md，本次新增/强化的：

1. **不许造假**：查不到官方价就显示「未开放/待确认」或标注「未核实」，**绝不硬塞一个数**。
2. **促销价必须有官方页原文或到期日兜底**，否则别写（`gpt-5.6-sol` 就是这么从 4/20 改回官方 5/30 的）。
3. **价格表按「原生币种」存储**：国内页给 CNY 就写 CNY 原值，海外给 USD 就写 USD 原值，**不要再做任何 ÷7**。
4. **`cacheHit` 缺官方佐证时别标「无缓存折扣」**（历史血泪：minimax-m2.7 长会话高估 5 倍）。
5. **加价格字段必须同步改 `convertPrice`**（§3.1③🔴）。
6. **改状态字段必须 bump `stateVersion`**（现为 **4**）。
7. **客户端改动需重启/刷新才生效**；容器里**别贸然重启 dsh web**（会断维护者会话）。
8. **推送前问维护者**。目前 `main` 领先 origin **2 个提交，未推送**。
